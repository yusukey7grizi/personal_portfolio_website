import { styled } from '@mui/material';
import { Colors } from 'components/constants';

export const WhiteSmokeWrapper = styled('div')({
  backgroundColor: Colors.whiteSmoke,
  paddingTop: '5rem',
  paddingBottom: '5rem',
  textAlign: 'center',
});

export const FlexBox = styled('div')({
  display: 'flex',
});
